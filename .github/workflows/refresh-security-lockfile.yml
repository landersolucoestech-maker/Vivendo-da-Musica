name: Refresh Security Lockfile (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Temporary workflow disabled
        run: echo "Security lockfile refresh completed; this workflow is intentionally disabled pending removal."
